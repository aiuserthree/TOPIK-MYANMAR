# 개발: Vite dev server proxy (/api → http://127.0.0.1:8000)
VITE_API_URL=/api

# 프로덕션(nginx /api 프록시): 빌드 시 같은 도메인 경로 사용
# VITE_API_URL=/api

# 프로덕션(절대 URL): 빌드 시 아래처럼 지정
# VITE_API_URL=https://YOUR_DOMAIN/api
