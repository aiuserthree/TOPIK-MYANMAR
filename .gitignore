.DS_Store
__pycache__/
*.pyc
.venv/

회의/
.vercel

# Node tooling (screenshot/export scripts)
node_modules/
scripts/node_modules/

# 대용량 디자인 산출물 zip (git 비추적 — 외부 저장소/전달본으로 관리)
시안/**/*.zip

# Static build outputs
public/
public-bo/
html/C*/BO/dist/

# Repo-root local secrets (never commit)
.env
api/.env.production

# AWS / local secrets (never commit)
topik-mm-s3-api_accessKeys.csv
*_accessKeys.csv

# Personal checklist — never commit
이것저것체크사항.txt

# API TypeScript build output
api/dist/

# Local/runtime upload storage (not versioned)
api/var/uploads/
